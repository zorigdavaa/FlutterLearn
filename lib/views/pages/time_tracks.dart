import 'package:flutter/material.dart';

class TimeTracks extends StatelessWidget {
  const TimeTracks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Time Tracks",
        style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),
      ),
    );
  }
}
